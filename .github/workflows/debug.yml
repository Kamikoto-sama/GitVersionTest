name: Debug

on:
  push:
    paths: .github/workflows/debug.yml

  workflow_dispatch:
    
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${git tag --contains ${{ github.sha }}}" >> $TAG
      - run: echo "$TAG"
