name: Debug

on: 
  push:
    paths:
      - .github/workflows/debug.yml
  workflow_run:
    workflows: "Tag with SemVer"
    types:
      - completed
  workflow_dispatch:
    inputs:
      ignore_diff:
        description: Aba asda
        type: boolean
        default: true
env:
  PATHS_REGEX: ^App1|debug.yml

jobs:
  debug:
    runs-on: ubuntu-latest
    if: inputs.ignore_diff
    steps:
      - name: Do work
        run: echo "${{ toJson(github.event.inputs) }}"
