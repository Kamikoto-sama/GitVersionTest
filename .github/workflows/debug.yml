name: Debug

on:
  push:
    paths: .github/workflows/debug.yml

  workflow_dispatch:

env:
  XXX: aboba

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set env
        run: echo "TAG=${git tag --containts ${{ github.sha }}}" >> $GITHUB_ENV
      - run: echo $TAG
