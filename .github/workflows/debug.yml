name: Debug

on:
  push:
    paths: .github/workflows/debug.yml

  workflow_dispatch:
    
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set env
        run: echo $GITHUB_ENV
